# Retomada do ERP — 08/09/2026

## Situação da recuperação

- Produção Vercel: `dpl_ABWuEdsxWBtoMB1nSHfe7Rc87py6`, publicada em 25/08/2026 00:18 UTC (24/08, horário de Brasília).
- Commit informado pela publicação: `a89bd3f5e003848d99ed04b10ad4c994c9f8338b`.
- O GitHub não encontrou esse commit; a branch `feat/esteira-comercial-v2-integrada` aponta para `f4c008caad5cb8ce032dd7574d34928448291602`.
- Os componentes `KitsModule.tsx`, `KitComposicaoClient.tsx` e `EstoqueClient.tsx` foram recuperados pelo botão Copy da aba Source da implantação. A cópia pela interface não preserva necessariamente a quebra de linha final.
- Essa recuperação é parcial. A branch deste trabalho não deve ser publicada nem integrada antes de reconciliar a fonte completa de produção.

## Ajuste preparado

Feedback aberto em 26/08: dificuldade para localizar kits em composição e estoque; fotos e descrições ocupam espaço demais.

- Kits e estoque: linhas compactas, miniaturas de 56 px, fotos completas e detalhes expansíveis.
- Kits: filtros de categoria/status, contagem sobre o total e limpeza de filtros.
- Composição: busca de kit por nome/código, seleção em lista e identificação persistente do kit selecionado.
- Busca compartilhada: ignora acentos e caixa, aceita várias palavras em qualquer ordem.
- Atalhos para as listas abaixo dos formulários de cadastro.
- Preservadas as ações de edição, retirada do catálogo, estoque e composição da fonte recuperada.
- Não houve alteração do banco nem publicação em produção. O feedback permanece aberto até validação.

## Verificação

`npx tsc --noEmit --incremental false` e `git diff --check` concluíram sem erros sobre a branch de trabalho. Ainda falta validação visual e funcional em homologação com a fonte completa de produção.

## Bloqueio para integrar

Recuperar o commit de produção no checkout original e enviá-lo para uma branch de recuperação, preservando a branch principal. Se o objeto estiver disponível no Mac:

```sh
git -C "$HOME/Developer/crm-cintia-paula-v6" push origin a89bd3f5e003848d99ed04b10ad4c994c9f8338b:refs/heads/recovery/production-20260824
```

Esse comando envia um commit existente; não captura alterações locais não commitadas. Se o objeto não existir, recuperar a pasta completa usada na implantação ou continuar a extração dos arquivos originais da Vercel. Não substituir essa recuperação por uma publicação da branch antiga.

Após recuperar a base: comparar os três componentes, aplicar apenas o commit do ajuste, validar em homologação isolada e concluir o feedback somente após aceite operacional.
